//! Shared test support (#38): the dockerized-RotorHazard harness — a `mock_data`
//! signal generator and a disposable container runner — reusable across crates'
//! `live` e2e tests. Populated by the harness agent.
